# 使用示例（dry-run）

1. 打开后台：`/admin-next/plugins/install`
2. 在“本地插件包 dry-run”区域输入：`examples/plugins/demo_notice_install`
3. 点击“扫描 / Dry-run”，查看：
   - 文件扫描结果（allowed / unknown / dangerous）
   - manifest 校验结果
   - 安装预览（install_preview）

说明：当前只做预览，不会真正安装插件包。

