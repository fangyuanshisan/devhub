# DevHub v1.8.3：后台插件治理页面稳定性与中文体验优化版

[返回文档入口](../README.md)

日期：2026-05-18

## 版本定位

v1.8.3 定义为“后台插件治理页面稳定性与中文体验优化版”。

本轮只优化后台插件治理页面稳定性、页面结构、页内 Tab、详情抽屉分组、中文文案、空状态、危险操作提示和安全边界说明；不修改插件底层生命周期逻辑，不修改 Webhook 协议，不修改 Secret / Callback Token 安全模型。
当前版本仍保持 `v1.8.3` 为主线版本口径；`external_service` 仅完成 non-blocking delivery 子集，完整第三方运行模型后续再做。

## 已完成

### v1.8.3-S23：插件系统发布前总验收与稳定性收口

- 完成 v1.8.3 插件系统总验收：覆盖 official_links 声明型插件完整链路、official_webhook_notify external_service Webhook 链路、upgrade dry-run / confirm / blocked / warning / failure_stage、声明型插件开发者指南与两个官方模板、frontend_mount 官方 allowlist、MemoryStore / MySQLStore 双模式、后台治理页面稳定性和文档一致性。
- 验收中修复 MySQLStore 新装 schema 缺口：`plugin_webhook_secrets.target_url` 与 `webhook_circuit_breakers.target_url` 原为 `VARCHAR(1000)` 且参与 `(plugin_code, target_url)` 相关索引，utf8mb4 下会超过 MySQL 3072 byte 索引长度限制；现收敛为 `VARCHAR(512)` 并同步 `db/mysql/001_schema.sql` 与 `internal/store/schema.go`。Webhook Secret 创建接口同步增加 `target_url` 512 字符上限中文校验；未参与索引的 `webhook_deliveries.target_url` 保持原长度。
- MySQL 模式通过 `./dev.sh start --mysql` 完整启动验收：MySQL ready、临时 API、前台构建、后台构建和最终 Go 服务均通过；health smoke 返回 `store=mysql`，后台登录、插件列表、审计列表和公共插件列表敏感字段剥离 smoke 通过。
- 检查：`gofmt -l`、`go test ./...`、`go build ./...`、插件包 / upgrade / external_service / frontend_mount 定向测试、`./scripts/build-plugin-package-fixtures.sh --suffix total-acceptance`、`./scripts/check-frontend.sh --admin-only --quick`、`./scripts/check-frontend.sh --frontend-only --quick`、`./scripts/check-admin-plugin-ia.sh`、`./dev.sh start --mysql` 和 `git diff --check` 均通过。后台 quick 日志目录 `.devhub/checks/20260521-140147/`，前台 quick 日志目录 `.devhub/checks/20260521-140207/`，插件 IA 回归截图目录 `.devhub/screenshots/plugin-ia`。
- 发布前边界确认：不新增插件市场、远程在线安装、动态代码执行、Go plugin、JS 沙箱、远程 iframe、remote component、任意远程 JS 或 blocking Hook；不改变 Webhook Secret / Callback Token / external_service token 安全模型；`migrations/` 仍是唯一迁移入口，dry-run 仍不执行 SQL。
- 结论：除完整第三方运行模型、插件市场、完整自动 rollback / migration down、生产大库回滚演练和更完整浏览器点击矩阵仍属于后续事项外，v1.8.3 插件系统建议进入冻结候选。

### v1.8.3-S22：前端插件挂载继续收口到官方 allowlist

- manifest 现在可声明 `frontend_mounts`，但只允许官方 allowlist 内的挂载点和官方组件 key；预检、install dry-run 和 upgrade dry-run 会阻断未知挂载点、未知组件 key、unsupported render_mode、`iframe_url`、`script_url`、`remote_entry`、`external_js`、`inline_html`、`remote_component`、`eval` 和未白名单的可执行 JS 资产。
- 运行时只渲染已安装、已启用 / running、未归档、未软卸载、当前子站可用且通过 allowlist 的挂载；未知历史组件会被跳过并记录 warning，不会白屏，secret / token / authorization / password / credential 类 props 不会传给前端组件。
- 2026-05-21 复查：运行时服务层只返回已安装、enabled / running、未 archived、未 soft_uninstalled，且当前子站 enabled 的插件挂载；props 会过滤 secret / token / authorization / password / credential 类字段，官方 helper 只创建内置同源 iframe。
- 后台插件详情“前端挂载”表格改为展示挂载点、组件 key、状态和说明；官方公告插件样板同步三个官方挂载位，并继续通过官方内置 helper 渲染。
- `upgrade dry-run` 已把 `frontend_mounts` 纳入差异和风险判断；新增未知挂载点或组件 key 会被 blocked，删除既有挂载会给出高风险提示。
- 当前不开放任意远程 iframe、远程 JS/CSS 入口、inline HTML 注入或第三方前端运行时；`HookBus` 仍只派发事件、记录执行和触发 external_service HTTP 投递，不执行第三方代码。
- 检查：`gofmt` 已执行；定向前端挂载单测、`go test ./...`、`go build ./...`、`git diff --check`、`./scripts/check-frontend.sh --admin-only --quick`、`./scripts/check-frontend.sh --frontend-only --quick` 均通过；前台 quick 仍生成站点和 topic 相关静态页面，本轮未改 `/topics/:id` SEO 动态 HTML，也未删除 sites/posts 兼容 API。前后台 quick 日志目录 `.devhub/checks/20260521-012440/`。

### v1.8.3-S21：声明型插件开发者指南与官方模板固化

- 新增 [声明型插件开发者指南](../PLUGIN_DEVELOPER_GUIDE.md)，把当前可用的声明型插件能力、不能做的运行时边界、插件包标准结构、manifest 编写指南、两个官方模板、安全规范、升级规范、常见错误和最小验收清单整理为长期入口。
- 新增纯声明型内容插件模板 `examples/plugins/templates/declarative-content/`，基于 `official_links` / 友情链接场景，包含 `manifest.json`、`README.md`、`config.example.json`、`PACKAGING.md` 和 `migrations/001_init.sql`，用于复制 content_type、permissions、menus、config_schema 和 migrations/ 的声明型插件。
- 新增 external_service Webhook 插件模板 `examples/plugins/templates/external-service-webhook/`，基于 `official_webhook_notify` 场景，包含 `manifest.json`、`README.md`、`config.example.json`、`receiver.example.md`、`PACKAGING.md` 和 `migrations/000_noop.json`，用于复制 external_service endpoint 配置、health check、non-blocking Hook、retry 策略和 mock receiver 验收流程。
- 2026-05-21 复查：开发者指南继续对齐当前实现，补充官方 allowlist 前端挂载边界和升级失败阶段 / 下一步建议说明；仍不执行第三方代码，不开放远程 iframe 或 blocking Hook。
- 2026-05-21 复查：开发者指南已成为声明型插件的默认起步入口，明确 `migrations/` 是唯一迁移入口、dry-run 不执行 SQL、`external_service` 仅是 non-blocking 子集；相关入口已同步到 `docs/README.md`、`README.md`、路线图和项目进度文档。
- 包扫描器允许根目录模板说明文件 `PACKAGING.md`、`package.example.md`、`receiver.example.md`，避免正式模板说明被误判为 unknown / dangerous；危险脚本、根目录 SQL、可执行代码、远程 iframe 和真实 secret 仍保持禁止或阻断。
- 新增 `TestDryRunPluginPackage_OfficialTemplatesOK`，固化两个模板必须通过当前 package dry-run、不含危险文件、不声明 blocking Hook；Webhook 模板必须声明 `AfterCreateContent` 的 `service_type=external_service` / `mode=non_blocking`。
- 本轮不改变 API、Webhook 协议、Secret / Token 安全模型、插件生命周期或前台 SEO；不执行第三方代码，不开放 Go plugin / JS 沙箱 / 远程 iframe / 插件市场 / 远程在线安装 / blocking Hook。
- 检查：`gofmt` 已执行；定向模板 dry-run 单测、`go test ./...`、`go build ./...`、`git diff --check` 均通过；本轮未修改后台前端运行入口，未执行后台 quick。

### v1.8.3-S19：external_service Webhook 插件可交付闭环

- 新增官方样板插件包 `examples/plugins/official_webhook_notify`，包含 `manifest.json`、`README.md`、`config.example.json`、`checksums.json` 和 `migrations/README.md`；声明 `AfterCreateContent` external_service non-blocking Hook，可用于 upload -> precheck -> promote -> install dry-run -> install，并配合 `cmd/webhook-mock-receiver` 验证投递。
- 新增手动重试 API：`POST /api/v1/admin/plugins/:code/hooks/executions/:execution_id/retry`，权限为后台 admin + `plugin.manage`。只允许 failed / timeout / retry_scheduled / retry_exhausted 的 external_service 执行记录重试；success、skipped、health_check、internal/builtin、跨插件 code 和 disabled / archived / soft_uninstalled 插件均拒绝。
- 手动重试复用 external_service HTTP 投递逻辑，创建新的 `hook_executions` 记录并标记 `manual_retry=true`、来源执行记录、operator 和 request_id；写入 `admin_logs` action=`plugin.webhook.manual_retry`，并按现有 health / failure_policy 规则更新 external_service 健康状态。
- 后台插件详情“运行记录”新增 external_service 配置表单：enabled、endpoint_url、health_check_path、timeout_ms、failure_policy、auth_type、token、warning_threshold、error_threshold，可保存配置并执行健康检查；Bearer token 只写入不回显，已有 token 显示“已配置密钥 / 可替换”。
- Webhook 治理新增“外部服务执行”Tab，可按插件编码查询 `hook_executions(service_type=external_service)`，失败类记录显示“重试”按钮；重试前提示外部服务需要幂等处理。
- 新增样板包 dry-run 守护测试 `TestDryRunPluginPackage_OfficialWebhookNotifyExampleOK`，确认 `official_webhook_notify` 可被现有 package dry-run 规则接受；同时修正 `currentCoreVersion()` 从子目录运行时误回退到旧版本的问题，改为向上查找仓库 `VERSION`。
- 本轮不改变 Webhook 协议、Secret / Callback Token 安全模型、插件生命周期或前台 SEO；不执行第三方代码，不开放 Go plugin / JS 沙箱 / 远程 iframe / 插件市场 / 远程自动安装 / 在线更新 / blocking Hook。

### v1.8.3-S20：插件升级 dry-run / confirm / 失败边界收口

- `POST /api/v1/admin/plugins/:code/upgrade/dry-run` 现在返回结构化升级计划：版本计划、包摘要、变更摘要、影响范围、迁移 / 配置 / 权限 / content_type / Hook / 前端挂载计划、风险项、阻断项、确认要求和回滚边界说明；仍不执行 SQL、不执行 migration down、不执行第三方代码。
- `POST /api/v1/admin/plugins/:code/upgrade` 和基于 compat-check 的升级都改为显式确认：warning 升级必须带 `confirm=true`，blocked 升级不能通过确认绕过；成功后继续写入审计，失败会记录 failure_stage / failure_reason。
- 后台升级向导改为“版本 / 变更 / 影响 / 回滚边界”结构化展示，原始 JSON 仅保留在技术详情折叠区；危险升级会要求勾选确认，blocked 升级直接禁用。
- 新增 Go 单测覆盖结构化 dry-run、secret redaction、warning confirm、same-version blocked 和 package upgrade confirm。
- 当前仍不提供完整自动回滚、migration down、配置版本回滚、assets 回滚或 external_service 配置回滚；升级失败后会尽量保持已安装版本可见，并记录失败阶段和审计日志，涉及数据库迁移的失败需要人工处理。
- 本轮不执行第三方代码，不开放远程 iframe、不开放 blocking Hook，不改变 Webhook 协议或 Secret / Token 安全模型。
- 检查：`gofmt` 已执行；`go test ./...`、`go build ./...`、`git diff --check`、`./scripts/check-frontend.sh --admin-only --quick` 均通过；后台 quick 日志目录 `.devhub/checks/20260521-000711/`。
- 2026-05-21 复查：warning 仍需显式确认，blocked 仍不可绕过；dry-run plan 过期或与当前包 checksum / migration plan 不一致时继续拒绝升级；升级失败继续返回 `failure_stage` / `failure_reason`，后台结果页仍展示当前版本、目标版本、风险等级、确认要求、阻断原因和下一步建议。
- 2026-05-21 复查：warning 仍需显式确认，blocked 仍不可绕过；dry-run plan 过期或与当前包 checksum / migration plan 不一致仍拒绝升级；升级失败继续返回 failure_stage / failure_reason，后台升级结果页仍显示当前版本、目标版本、风险等级、确认要求、阻断原因和下一步建议。

### v1.8.3-S18 追加：整理 Webhook 插件使用方法

- 新增 `docs/PLUGIN_WEBHOOK_USAGE.md`，把当前已实现的 Webhook 插件子集整理成可执行使用方法：external_service non-blocking 投递、Webhook Secret、Callback Token、健康检查、投递记录、异常处理和排障入口。
- 该文档明确当前仅实现 non-blocking delivery 子集，完整第三方运行模型仍未实现；不执行第三方代码，不开放 blocking Hook，不开放远程 iframe URL。
- 本次只改文档，不改 API、不改 Webhook 协议、不改 Secret / Token 安全模型。

### v1.8.3-S18：插件后台按钮行为与旧路由跳转回归修复

- 修复插件详情抽屉内治理跳转继续携带 `/admin-next` 导致在 `createWebHistory('/admin-next/')` 下跳错的问题；当前跳转统一走 `/plugins/webhooks`、`/plugins/overview`、`/plugins/runtime`、`/audit-logs` 和插件内容页相对后台 base 的路径。
- 修复详情抽屉低频 Tab 被压缩后旧入口无可见结果的问题：Webhook、Webhook 密钥、回调 Token、前端挂载、审计、Hook、readiness、dependencies、content_type、permissions、migrations、routes 等入口映射到“能力 / 运行记录 / 技术详情”，并显示中文说明，避免空白抽屉或静默失败。
- 修复旧路由到新治理域 / Tab 的兼容：补齐 `/plugins/package-uploads`、`/plugins/remote-packages`、`/plugins/webhook-events`、`/plugins/webhook-deliveries`、`/plugins/webhook-retry`、`/plugins/webhook-circuits`、`/plugins/webhook-secrets`、`/plugins/callback-tokens`、`/plugins/callback-requests` 等 redirect。
- Webhook 治理对 `retry/circuits/callback-tokens/callback-requests` 做 Tab 归一，最终定位到 `exceptions/callback_tokens/callback_requests`；刷新和浏览器前进 / 后退能保持 Tab 状态。
- 插件总览、插件包治理、发布者与信任、运行记录 / 审计的低频 Tab 在通过旧路由或 query 进入时会临时显示对应 Tab，不再落到隐藏状态。
- 复查详情抽屉隐藏旧技术入口后，技术详情按“启用检查 / 迁移明细 / 导出本地插件包 / 原始声明 JSON”重新做最小分组；启用检查和迁移明细不再依赖隐藏旧 Tab，导出入口保持可见，原始声明继续默认折叠并脱敏。
- 迁移重试按钮补齐前端 API import，避免按钮可见但点击时报 `retryPluginMigration` 未定义；迁移耗时和 rollback 字段兼容后端不同字段名。
- 插件总览和详情抽屉文案改成任务导向：用户现在先按“要安装 / 启用 / 排障 / 看审计 / 管密钥 / 看原始声明”判断入口，再进入对应治理域，不再只靠页面名猜位置。
- 插件列表按钮文案也统一成任务语言：主按钮变成“校验清单 / 查看预检 / 去安装”，行内动作变成“看详情 / 处理配置 / 更多任务 / 去审计 / 去启用 / 去停用 / 做启用检查 / 去软卸载 / 恢复入口”，让管理员不再在一个表格里同时读功能名和任务名。
- `scripts/check-admin-plugin-ia.sh` 扩展 S18 浏览器回归：覆盖 5 个治理域、主要按钮点击、详情抽屉配置 / 能力 / 运行 / 技术详情、旧路由映射、标题 / 面包屑 / 页面不白屏和中文占位。
- 本轮只改后台 UI / 路由 / 回归脚本和文档；未改变 API、插件底层逻辑、Webhook 协议、Secret / Token 安全模型，未开放远程 iframe、第三方代码执行或 blocking Hook。

### 当前版本口径统一

- `VERSION` 已统一为 `v1.8.3`。
- README、docs/README、AGENT_RULES、PROJECT_PROGRESS、PLUGIN_ARCHITECTURE、PLUGIN_SYSTEM_ROADMAP、PLUGIN_PACKAGE、PLUGIN_WEBHOOK_IMPLEMENTATION_PLAN、CHANGELOG 等入口文档已同步把“当前版本 / 当前主线 / 当前范围”收敛到 v1.8.3。
- 历史 v1.7.x / v1.8.0-v1.8.2 文档仍保留为追溯背景，不作为当前 Codex 任务口径。
- 本次只更新版本声明和文档口径，不改变插件生命周期、Webhook 协议、Secret / Token 安全模型，不开放第三方代码执行、远程动态加载、远程 iframe、插件市场或 blocking Hook。

### v1.8.3-S16：后台插件治理复杂度继续压缩

- 左侧插件导航继续保持 5 个治理域：插件总览、插件包治理、Webhook 治理、发布者与信任、运行记录 / 审计；不恢复 20+ 低频入口长目录。
- 插件总览默认只保留高频任务入口：概览摘要、插件列表、待处理事项和快捷操作；配置中心、前端挂载、内容治理、权限矩阵、开发者工具等低频能力收敛到“高级治理”入口。
- 插件包治理改为流程型工作台：默认呈现上传 / 暂存、预检、转入本地仓库、安装 dry-run、安装 / 升级、结果 / 审计的顺序，阻断原因和下一步动作放到显眼位置；历史、远程索引、依赖兼容性和审批保留为低频入口。
- Webhook 治理默认聚焦总览、投递记录和异常处理，直接回答“是否发出、失败在哪里、是否需要重试 / 熔断恢复 / 检查密钥”；Webhook 密钥、回调 Token、回调请求、原始 payload 和技术 JSON 进入“高级治理”。
- 发布者与信任默认突出发布者名称、可信级别、状态、影响范围、最近使用和操作，弱化 `publisher_id` / `key_id` / 公钥详情等技术字段。
- 运行记录 / 审计只作为排障和追踪入口，默认展示最近错误、操作历史和审计日志；Hook 排障、搜索索引、原始 metadata 和技术 trace 进入高级入口或折叠区。
- 插件详情抽屉进一步压缩为“概览 / 配置 / 能力 / 运行 / 技术详情”：Webhook、权限、content_type、external_service 只展示当前插件摘要和跳转入口，不复制全局治理大表；技术详情和原始 JSON 默认折叠。
- `official_announcement` 详情继续保留配置、挂载和预览入口；前端挂载说明已随“能力”Tab 聚合，继续提示只允许内置 iframe、`sandbox=allow-scripts`、postMessage 通信、不暴露 callback token / webhook secret。
- `scripts/check-admin-plugin-ia.sh` 已同步新的详情抽屉结构，浏览器回归覆盖 5 个治理域、旧路由跳转、1024 宽度详情抽屉、普通插件、`official_announcement` 和敏感字段检查；截图目录 `.devhub/screenshots/plugin-ia`。
- 本轮只调整后台 UI / IA / 回归脚本和文档，不改变 API、插件底层逻辑、PluginRegistry reload、Webhook 协议、Secret / Token 安全模型，不开放远程 iframe，不执行第三方代码，不做 blocking Hook。

### v1.8.3-S15：真实声明型插件“安装到使用”完整业务闭环验收

- 新增真实声明型插件 fixture：`scripts/build-plugin-package-fixtures.sh` 可生成 `devhub-fixture-links-plugin*.zip`。插件编码为 `official_links*`，中文名为“声明型友情链接插件”，声明 `friend_link*` content_type、4 个插件权限、后台 / 前台菜单、配置 Schema、默认配置和 `migrations/001_init.sql` 最小业务表。
- 该 fixture 不包含 package scripts、远程代码、Go / Node / PHP 可执行资产、`.so/.dll/.node/wasm`、远程 iframe URL 或根目录 `001_schema.sql`；DevHub 只解析 manifest、执行受控 migrations 计划和注册声明能力，不执行第三方代码。
- 新增真实 Admin API E2E `web/admin-app/tests/e2e/plugin-declarative-install-use.spec.js`，覆盖 upload -> precheck -> promote -> local repository -> install dry-run -> install -> PluginRegistry reload -> enable -> community enable 的安装链路。
- 验收确认 install 前必须重新 dry-run，upload 阶段 dry-run 不可替代 install dry-run；dry-run 不执行 SQL、不写安装状态，install 只基于 `migrations/`，不执行根目录 `001_schema.sql` 或 package scripts。
- 验收确认安装后插件默认 disabled；启用后声明菜单、`friend_link*` content_type、权限矩阵、配置读写和菜单预览均进入 Core 治理链路。
- 后台兼容创建入口 `POST /api/v1/admin/posts` 支持在后台受控场景显式携带 `category_id/content_type/plugin_code` 创建声明型内容，但仍统一进入 `Service.CreateTopic` 校验插件全局状态、子站状态、板块绑定、`allowed_content_types` 和插件 create 权限；公开前台发帖入口不会接受测试权限覆盖。
- 验收确认允许板块可创建 `friend_link*` 内容；不允许板块、子站 disabled、全局 disabled、archived 均会阻断新建；归档后历史内容仍可通过 `/api/v1/topics/:id` 读取并保留 `plugin_code/content_type`。
- 后台页面 smoke 确认插件总览可搜索该插件，本地仓库可看到对应包；审计记录包含安装、状态变更、配置保存和归档事件。
- 本轮仍不执行第三方代码、不开放动态加载、不开放远程 iframe、不实现 blocking Hook，不改变 Webhook 协议或 Secret / Token 安全模型。

### v1.8.3-S14：external_service non-blocking Webhook 投递闭环

- 声明型插件 Hook 声明新增 external_service 投递字段：`service_type=external_service`、`path`、`method`、`retry_enabled`、`max_attempts`、`enabled`；manifest 校验强制 external_service Hook 只能使用 `mode=non_blocking`，Hook `path` 必须是相对路径，第一版只支持 `POST`。
- HookBus 触发后会创建 `hook_executions(service_type=external_service)` 的 `pending` 记录，并在后台 goroutine 中异步投递 `POST {endpoint_url}{hook.path}`；主业务流程不等待远端响应，external_service 失败不会阻断内容发布 / 配置保存 / 用户操作主流程。
- 投递请求包含 `X-DevHub-Plugin-Code`、`X-DevHub-Hook-Name`、`X-DevHub-Execution-ID`、`X-DevHub-Event-ID`、`X-DevHub-Request-ID`、`X-DevHub-Idempotency-Key`、`X-DevHub-Timestamp`、`X-DevHub-Delivery-Mode: non_blocking`，payload 使用 schema_version=1 并携带资源、子站、actor 和事件追踪信息。
- 支持 `auth_type=none|bearer`、`timeout_ms`、`retry_enabled/max_attempts`、`failure_policy=ignore|warn|error|disable_hook`；timeout、5xx 和 429 可进入 `retry_scheduled`，超过次数进入 `retry_exhausted`，4xx / 401 / 403 / token 缺失默认不重试。
- 投递结果进入 `hook_executions`：记录 `pending/running/success/failed/timeout/retry_scheduled/retry_exhausted/skipped`、response_status、duration_ms、request_body_sha256、error_code、error_message、execution_id、event_id、idempotency_key、attempt、next_retry_at 和 health before/after。
- 失败会按阈值更新 external_service health warning / error，成功会恢复 healthy 并清零 failure_count；`disable_hook` 到 error 后后续投递记录 skipped，不自动禁用插件。
- 插件 disabled / archived / soft_uninstalled、external_service disabled、子站 disabled、endpoint 缺失或 token 缺失时不调用 endpoint，并写入 skipped / failed 执行记录和中文原因。
- 复用已有 `GET /api/v1/admin/plugins/:code/hooks/executions?service_type=external_service` 与后台详情抽屉 / 运行记录入口查看投递结果；本轮未新增手动 retry API。
- token 明文、Authorization Header、Webhook Secret、Callback Token、敏感请求体不进入接口响应、执行记录、日志或审计；本轮仍不执行第三方代码、不开放动态加载、不开放远程 iframe、不实现 blocking Hook。

### v1.8.3-S11：external_service Webhook 运行时预备

- 新增 external_service 运行时配置模型：`plugin_external_services` 保存 `plugin_code`、`service_type=external_service`、`endpoint_url`、`health_check_path`、`timeout_ms`、`failure_policy`、`auth_type`、`token_ref`、健康状态、最近检查时间和失败计数；MemoryStore / MySQLStore 均支持读写。
- 新增 Admin API：`GET /api/v1/admin/plugins/:code/external-service`、`PUT /api/v1/admin/plugins/:code/external-service`、`POST /api/v1/admin/plugins/:code/external-service/health-check`；`GET /plugins/:code/hooks/executions` 支持 `service_type=external_service` 筛选。
- endpoint 校验只允许 HTTPS，本地开发允许 `http://localhost`、`http://127.0.0.1`、`http://[::1]`；拒绝 `javascript:`、`data:`、`file:`、`ftp:`。`timeout_ms`、`failure_policy`、`auth_type` 均有白名单和默认值。
- Bearer token 使用插件配置 keyring 加密保存并生成 `token_ref`；接口、后台详情、审计、日志、`hook_executions` 均不回显 token 明文、Authorization Header、Webhook Secret 或 Callback Token。
- health check 执行受控 `GET {endpoint_url}{health_check_path}`，2xx 为 healthy，3xx 记 warning，4xx/5xx/timeout/DNS/TLS/连接错误按失败策略累计；达到阈值后进入 warning / error，成功后恢复 healthy 并清零 failure_count。
- health check 结果统一写入 `hook_executions(service_type=external_service, hook_name=external_service.health_check)`，便于运行记录 / 审计和插件详情排障。
- 插件 disabled / archived / soft_uninstalled 时 external_service 不调用 endpoint，只记录 skipped；external_service 状态进入插件健康摘要，后台详情抽屉展示外部服务地址、健康状态、超时、失败策略、认证方式、失败次数、最近检查和最近失败原因。
- 2026-05-21 复查：后台 external_service 配置表单继续位于插件详情抽屉“运行记录 / 外部服务配置”，Webhook 治理页“外部服务执行”用于查看投递记录和失败重试；定向 `TestExternalServiceHealthCheckWarningAndRecovery|TestExternalServiceValidationAndDisabledPluginSkipped` 通过。
- 本轮不执行第三方插件代码，不开放动态加载，不开放远程 iframe URL，不做 JS 注入，不做 blocking Hook，不改变 Webhook Secret / Callback Token 安全模型。

### v1.8.3-S10：声明型插件可用闭环

- 声明型插件安装后的 manifest 能力继续进入 PluginRegistry / Service 运行态：`content_types`、`content_type_definitions`、`permissions`、`menus`、`config_schema` 等可信元数据可被后台治理、发布校验和子站启用链路读取。
- MemoryStore / MySQLStore 的子站插件列表、子站启停、子站配置、排序现在支持已安装的声明型插件，不再只认内置 registry 插件。
- 内容发布闭环补齐：声明型插件 `content_type` 可以创建对应板块，发布时必须同时满足全局 enabled、子站 enabled、板块 allowed_content_types、plugin_code 归属和 `create_permission`；全局 disabled、子站 disabled、archived / soft_uninstalled 均阻断新内容。
- 内容存储与读取保留声明型插件归属：创建 / 更新内容时尊重 Service 层解析出的 `plugin_code`，MemoryStore 与 MySQLStore 读取历史内容时不再把非内置 content_type 强行映射回 core。
- 权限矩阵闭环补齐：`Service.AdminPermissions()` 会把运行态插件声明的权限按 `plugin_code` 分组追加到后台权限矩阵，供后台治理页展示和后续 RBAC 分配使用。
- 新增 `TestDeclarativePluginManifestCapabilitiesClosedLoop` 覆盖声明型插件安装、启用、子站启用、菜单声明、content_type 创建、权限矩阵、子站停用、全局停用、归档阻断和 Registry reload 后状态立即生效。
- 本轮不执行第三方代码，不开放动态加载或远程 iframe，不改变 Webhook 协议、Secret / Token 安全模型或 blocking Hook 边界。

### v1.8.3-S9：PluginRegistry reload 运行态刷新收口

- Service 层新增统一 `RefreshPluginRegistry` / `refreshPluginRegistry` 收口点，并维护插件运行态快照；当前 DevHub 不引入新的动态加载框架，刷新只重读可信元数据并原子替换快照。
- 安装、升级、启用、停用、软卸载 / 归档、恢复、全局配置变更、子站插件启停、子站配置变更成功后触发 reload；配置密钥轮换写回配置时也触发配置变更 reload。
- reload 成功后 `IsPluginEnabled`、`IsPluginEnabledForCommunity`、`PluginByCode`、`CommunityPlugins`、`ResolvedConfig` 通过新快照立即反映状态和配置。
- 全局 reload 会清空已缓存的子站视图，避免全局停用后旧子站快照仍显示 enabled；子站 reload 只更新对应子站快照。
- reload 失败保留旧快照，并写入 `plugin.registry.reload.failed` 审计；数据库写入失败、安装 / 升级失败、迁移记录写入失败后的回滚路径不刷新脏运行态。
- reload 不执行第三方插件代码，不读取远程代码，不开放动态加载，不改变插件生命周期语义、Webhook 协议、Secret / Token 安全模型或 blocking Hook 边界。

### v1.8.3-S7：official_announcement 浏览器回归固定测试种子 / fixture

- `scripts/check-admin-plugin-ia.sh` 新增 `official_announcement` fixture 前置步骤：先通过 `/api/v1/admin/login` 获取真实后台 token，再用现有后台插件 API 幂等确保官方公告插件全局启用、PHP 或首个可用子站启用。
- fixture 默认配置固定为：`enabled=true`、`message="欢迎使用 DevHub 官方公告插件"`、`link_text="查看详情"`、`link_url="/"`、`dismissible=false`。
- fixture 多次执行时会先比较全局 / 子站配置；配置一致时跳过重复写入，减少配置版本和审计噪音。
- 浏览器回归不再在缺少数据时跳过 `official_announcement`，而是强制覆盖插件列表、详情概览、公告配置、前端挂载、公告预览和 1366 宽度详情截图；报告会记录 fixture 准备结果。
- 回归脚本校验公告预览 iframe 仍使用内置 `/plugins/official-announcement/iframe`，`sandbox="allow-scripts"`，且 iframe DOM 不包含 callback token、webhook secret、token_hash、authorization、secret/token query。
- 修复官方挂载 Host helper 后台预览鉴权：admin area 读取 context / 写 audit 时使用后台 sessionStorage 中的 admin token 作为 Host 请求 Authorization，但 token 仅留在 Host 层，不传入 iframe，不改变 Host + iframe + postMessage 协议。
- fixture 通过现有后台 API 适配 MemoryStore 与 MySQLStore；不新增生产 API，不改变插件底层生命周期、Webhook 协议、Secret / Token 安全模型，不开放远程 iframe，不执行第三方代码，不做 blocking Hook。

### v1.8.3-S8：插件包 migrations 规范收口

- 插件包数据库迁移入口统一为 `migrations/`；dry-run / 预检 / 安装 / 升级只读取 `migrations/` 下的 `.sql` 文件并按文件名排序生成只读 `migration_plan`。
- 根目录 `001_schema.sql` 进入 deprecated 兼容 warning，不再作为标准迁移入口，也不会在 dry-run / 安装 / 升级流程中执行。
- 根目录其他 `.sql` 文件继续阻断，避免插件包根目录任意 SQL 被误认作迁移入口。
- 新模板 / 示例统一生成 `migrations/001_init.sql`；`plugin:new` 仍以声明型插件骨架为主，不再生成根目录 `001_schema.sql`。
- dry-run 仍只做解析、校验、计划生成和风险提示，不会执行 SQL、修改数据库或写入插件安装状态。
- 本轮只收口插件包迁移规范，不引入新 migration 框架，不改变插件生命周期、Webhook 协议或 Secret / Token 安全模型。

### v1.8.3-S6：插件详情抽屉性能拆包与 1024 宽度视觉回归

- `PluginConfigVersionsDialog` 从详情抽屉主路径改为 `defineAsyncComponent` 按需加载，配置版本弹窗打开前不进入详情抽屉首屏同步加载路径。
- 技术详情拆成异步组件 `PluginTechnicalDetails`，技术详情 Tab 进入时再加载；详情抽屉的配置、前端挂载、Webhook、安全凭据、运行记录、审计日志、技术详情等低频 Tab 使用 `lazy`，减少打开抽屉时的初始化压力。
- 配置编辑器中的 `json-editor-vue` 改为仅在用户切到 JSON 模式时动态加载；默认表单模式不再同步拉取重型 JSON 编辑器。
- 配置版本弹窗宽度改为 `min(94vw, 980px)`，详情 / 回滚预览抽屉改为 `min(94vw, 760px)`；列表和 diff 表格补充中文空状态，JSON / diff 折叠标题改为中文。
- 构建拆包结果：`PluginConfigVersionsDialog` 独立为约 9.90KB chunk，`PluginTechnicalDetails` 独立为约 0.70KB chunk，`PluginConfigEditor` 从约 1.16MB 降到约 134KB；剩余大 chunk 主要是后台主入口、内容页和按需 `json-editor-vue`。
- 扩展 `scripts/check-admin-plugin-ia.sh`：除 5 个治理域和旧路由外，新增 1024 宽度普通插件详情、配置 Tab、技术详情折叠、配置版本弹窗截图；1366 宽度继续回归治理域页面。
- 浏览器回归截图目录：`.devhub/screenshots/v1.8.3-s6`。当前测试数据未返回 `official_announcement`，脚本记录跳过原因，未伪造通过。
- 本批次只调整后台 UI、懒加载组织、回归脚本和文档，不改变 API、插件生命周期、Webhook 协议、Secret / Token 安全模型，不开放远程 iframe、第三方代码执行或 blocking Hook。

### v1.8.3-S5：插件详情抽屉视觉 polish 与信息减负

- 插件详情抽屉可见 Tab 收敛为：概览、配置、前端挂载、Webhook、安全凭据、运行记录、审计日志、技术详情；低频技术入口不再与高频治理入口平级堆叠。
- 概览 Tab 只展示插件身份、状态、能力摘要和风险边界，补充“是否执行第三方代码：否”“允许远程 iframe URL：否”“Webhook 能力”“回调 Token 不展示明文”等摘要。
- 配置 Tab 改为生效配置摘要 + 编辑入口：展示配置来源、生效字段、配置模型和校验状态；原始配置模型、当前配置 JSON、生效配置快照统一进入技术详情。
- 前端挂载 Tab 补充 iframe 路由、sandbox、postMessage、挂载位置、远程 URL、第三方代码执行和凭据暴露说明；`official_announcement` 可直接进入公告预览。
- Webhook Tab 仅保留当前插件摘要和跳转入口，不复制全局 Events / Deliveries / Retry / Circuit Breaker 表格。
- Webhook 密钥和回调 Token 收敛到“安全凭据”Tab，明确 secret_ref / token_ref、权限范围、轮换 / 禁用 / 吊销入口以及明文只展示一次的规则。
- 技术详情 Tab 默认折叠，收纳 config_schema、resolved_config、config_json、内容类型、权限、前端挂载声明、Webhook / Hook 声明、依赖和运行健康原始摘要；展示前对 token / secret / authorization / signature / token_hash / hmac 字段脱敏。
- 审计详情和 Hook 执行详情中的 JSON 元数据展示同样做敏感字段脱敏，避免 token_hash、Authorization Header 或完整 HMAC signature 在详情抽屉中裸露。
- `official_announcement` 的公告配置、前端挂载、公告预览入口更直观，继续说明官方内置、只允许内置 iframe、不执行第三方代码、不暴露 callback token / webhook secret。
- 本批次只调整后台 UI 和文档，不改变 API、插件生命周期、Webhook 协议、Secret / Token 安全模型，不开放远程 iframe、第三方代码执行或 blocking Hook。

### v1.8.3-S4：插件后台总览页布局修正与筛选体验优化

- 修正插件总览页视觉留白和内容偏右问题：插件页内容区改为自适应宽度，避免在 1366 宽度下被固定 `max-width` 挤到右侧；1024 宽度下也不产生横向溢出。
- 插件总览页首屏重新排版：快捷操作压缩为“插件列表 / 安装升级 / 更多治理”，健康摘要默认收起，最近异常保留为首屏排障入口，统计卡高度降低。
- 插件列表筛选区改为紧凑横向筛选栏：搜索、状态、健康、内容类型、能力、重置、刷新默认可见；系统插件和配置模型等低频条件沉到“高级筛选”，默认折叠。
- 批量操作面板仅在选中插件后显示，避免未选择时占用首屏；插件列表主表格更靠前。
- 插件详情抽屉继续收敛 official_announcement 入口：在头部和概览中直接提供“公告配置 / 前端挂载 / 公告预览”快捷入口，并继续说明官方内置、只允许内置 iframe、不执行第三方代码、不暴露 callback token / webhook secret。
- 新增轻量插件 IA 回归脚本 `scripts/check-admin-plugin-ia.sh`，用于一键检查 5 个治理域、旧路由到 Tab 跳转、页面标题/面包屑、中文空状态和 1024 宽度溢出；该脚本不替代完整 E2E。
- 中文文案第二轮扫描覆盖筛选、按钮、空状态、分页和详情入口；`No Data`、`20/page` 未在本轮浏览器回归中裸露。
- 本批次只修改后台 UI、脚本和文档，不改变 API、插件生命周期、Webhook 协议、Secret / Token 安全模型，不开放远程 iframe、第三方代码执行或 blocking Hook。

### v1.8.3-S2：后台插件二级导航收敛与三级 Tab 重组

- 左侧插件导航最终收敛为 5 个治理域：插件总览、插件包治理、Webhook 治理、发布者与信任、运行记录 / 审计，不再直接展示 20+ 个低频入口。
- 新增治理域容器页：`PluginOverviewDomain`、`PluginPackagesDomain`、`PluginPublishersDomain`、`PluginRuntimeDomain`，通过页内 Tab 承接原入口。
- 插件总览域包含：总览、插件列表、配置中心、前端挂载、内容治理、权限矩阵、开发者工具。
- 插件包治理域包含：远程索引、远程包下载、暂存上传包、本地包与预检、依赖 / 兼容性、版本与升级、安装 / 升级审批；并保留远程索引、暂存区、预检、启用前检查、升级不执行第三方代码的说明。
- Webhook 治理域新增“Webhook 总览”Tab，并继续保留事件通知、投递记录、重试队列、熔断状态、Webhook 密钥、回调 Token、回调请求；空数据安全默认值仍保留。
- 发布者与信任域包含：发布者列表、公钥 / key_id、可信级别、影响分析、密钥轮换；依赖 / 兼容性已归入插件包治理。
- 运行记录 / 审计域包含：操作历史、审计日志、Hook 排障、搜索索引、最近错误；最近错误只提供排障入口说明，不重复展示完整全局表格。
- 旧插件路由保持兼容并重定向到新治理域和对应 `?tab=`：例如 `/plugins/list` -> `/plugins/overview?tab=list`，`/plugins/install` -> `/plugins/packages?tab=install`，`/plugins/events` -> `/plugins/webhooks?tab=events`，`/plugins/trusted-publishers` -> `/plugins/publishers?tab=list`，`/plugins/operations` -> `/plugins/runtime?tab=operations`。
- `PluginInstallUpgrade` 的内部工作区 Tab 改为 `workspace_tab`，避免与治理域三级 Tab 的 `tab` query 冲突；旧 `/plugins/install?tab=template` 会兼容到 `/plugins/packages?tab=install&workspace_tab=template`。
- 插件后台公共筛选条改为“标题说明在上、筛选条件网格排布、按钮随条件同行”的紧凑布局，避免宽屏下输入框被拉满成纵向长条。
- 插件模块保留左侧二级导航，但调整为“插件管理”分组下直接展示 5 个治理域入口，避免 5 个单项分组造成左侧栏视觉空洞。
- 本批次只调整后台页面 IA、导航、Tab 与中文说明，不改变插件生命周期、Webhook 协议、Secret / Token 安全模型，不开放远程 iframe、第三方代码执行或 blocking Hook。

### v1.8.3-S1：后台插件治理页面稳定性修复与 IA 第一批收敛

- Webhook 治理页继续使用安全列表响应归一化，接口返回 `null` / 缺少 `items` 时仍显示中文空状态，不再因为 `.length` 读取导致白屏。
- 插件详情抽屉补齐 `maturityLabel` 导入，并增加有效插件兜底；无效插件对象不会继续渲染详情主体，依赖列表也过滤空依赖和空插件项。
- 插件列表数据源在 `usePluginData` 中过滤 `null` / 缺少 `code` 的插件项，避免概览、列表和详情入口读取 `plugin.code` 报错。
- 左侧插件导航维持 5 个治理域：插件总览、插件包治理、Webhook 治理、可信发布者、运行记录 / 审计；低频入口继续归入对应治理域。
- 插件列表做第一批减负：健康摘要默认折叠，首屏表格保留插件、版本、能力摘要、最近操作、操作等关键列；插件类型、状态、健康和官方内置标识合并到插件主列展示。
- `official_announcement` 在插件列表中显示“官方公告插件”和“官方内置”标识，能力摘要突出前端挂载、配置和 iframe 预览。
- 上传包详情默认先显示状态摘要、建议动作和可用操作，原始 zip 扫描、清单文件校验、风险报告、错误详情、安装预检 JSON 折叠到“技术详情”。
- 可信发布者页面做信息密度优化：主列表突出发布者、可信级别、状态、影响范围和操作，`publisher_id` / `key_id` 弱化到发布者主列，指纹和公钥保留在详情中。
- 插件包治理和上传包相关文案继续统一为“预检 / 清单文件 / 校验和 / 暂存区 / 本地仓库”等中文后台语义。
- 修复 `/admin-next/plugins/operations` 操作历史页响应解包错误：兼容已由 `http` 拦截器解包的业务响应与 axios `{ data }` 响应，列表返回空对象 / 空响应时显示“暂无操作记录”，不再读取 `undefined.items`。

### v1.8.3 主体收敛

- 修复 Webhook 治理页空数据 / 缺字段时的白屏风险：Events、Deliveries、Retry、Circuit Breakers、Secrets、Callback Tokens、Callback Requests 均通过安全默认值和中文空状态渲染。
- 修复插件详情抽屉运行时异常：补齐 `maturityLabel` 导入，并避免配置版本弹窗在插件对象为空时读取 `plugin.code`。
- 左侧插件导航收敛为 5 个治理域：插件总览、插件包治理、Webhook 治理、可信发布者、运行记录 / 审计；旧路由仍保留并归入对应治理域。
- 插件详情抽屉新增更清晰的三级 Tab 分组：概览、配置、前端挂载、Webhook、Webhook 密钥、回调 Token、运行记录、审计日志等能力不再全部平铺在首页。
- 插件列表页补齐插件类型、能力摘要、子站启用情况、最近操作、查看详情/配置/启用/停用/软卸载/升级/查看审计等中文操作入口。
- Webhook 治理页按场景拆分为：事件、投递记录、重试队列、熔断状态、Webhook 密钥、回调 Token、回调请求。
- 插件包治理页保留“本地仓库 / 初始化插件包 / 上传 zip”页内 Tab，并补充远程索引、暂存区、预检、兼容性检查、启用前检查、升级不会执行第三方代码等边界说明。
- 上传记录、远程插件包、审批中心、操作历史、配置版本历史/回滚预览等治理页面补齐中文字段、中文状态、中文按钮、空状态和高风险动作确认。
- 可信发布者页将状态、可信级别、允许/阻断插件、过期时间、最近使用、按钮、确认弹窗和详情字段中文化，明确发布者变更会影响包验签与安装准入。
- `official_announcement` 在插件详情中补充官方内置、前端挂载模型验证、不执行第三方代码、仅允许内置 iframe 页面、不暴露 callback token / webhook secret 等说明。
- 补齐统一状态展示：enabled / disabled / retry_scheduled / retry_exhausted / circuit_open / half_open / revoked / expired 等后台状态以中文 badge 展示。
- `dry-run` 在用户可见操作中统一表述为“预检”，技术字段和 JSON key 仍保留原值以便排障。
- 修复 `scripts/check-frontend.sh` 在 Docker named volume 已被 root 创建时无法写入 `node_modules/.vite-temp`、`test-results`、`playwright-report` 的问题：检查脚本会在运行前用 root 容器修正对应 workspace 目录属主，再按当前 UID/GID 执行实际构建与 E2E。

## 安全边界

- 仍不开放远程 iframe URL。
- 仍不执行第三方插件代码。
- 仍不做 JS 注入。
- 仍不开放插件市场。
- 仍不做 blocking Hook。
- Webhook Secret 和 Callback Token 明文仍只展示一次，列表和详情不展示明文。
- Callback Token 仍不是管理员权限，只能访问授权 Scope。
- 停用插件会停止前端挂载和 Webhook 投递；软卸载保留历史数据但停止新能力。

## 未改变

- 插件生命周期底层逻辑未改变。
- Webhook delivery / retry / circuit breaker 核心逻辑未改变。
- Secret / Callback Token 安全模型未改变。
- Host + iframe + postMessage 协议未改变。
- `/topics/:id` SEO、`/c/:slug` SEO、`sites/posts` 兼容 API 未改变。

## 后台插件中心中文状态和异常提示统一

- 新增插件模块集中映射 `web/admin-app/src/modules/plugins/statusText.js`，统一状态、风险、阻断原因、Hook / 健康原因、操作名和建议文案。
- 插件列表、详情抽屉、插件包上传 / promote / install、远程索引、版本升级、配置中心、内容治理、配置密钥、Hook、依赖、配置版本回滚等后台插件页面接入统一中文展示。
- 前端 HTTP 错误提示优先展示后端中文 `message`；旧接口仅返回 `code` 时使用插件映射兜底，同时保留“错误码：xxx”给排障和自动化测试。
- 后端插件接口的 `APIError.code`、业务状态枚举和兼容结构保持不变；本轮不引入完整 i18n 框架，不修改插件生命周期、Webhook 协议、Secret / Token 安全模型，不开放远程 iframe、第三方代码执行或 blocking Hook。

## 检查记录

本轮后台改动完成后已执行：

```bash
./scripts/check-frontend.sh --admin-only --quick
```

结果：

- v1.8.3-S18：插件后台按钮行为与旧路由跳转回归修复。`./scripts/check-admin-plugin-ia.sh` 通过，覆盖 5 个治理域、主要按钮点击、详情抽屉配置 / 能力 / 运行记录 / 技术详情、旧路由到新治理域 / Tab、标题 / 面包屑 / 页面不白屏；截图目录 `.devhub/screenshots/plugin-ia`。`./scripts/check-frontend.sh --admin-only --quick` 通过，后台 quick 日志目录 `.devhub/checks/20260520-202752/`。后台构建仍提示既有 `index/Content/json-editor-vue` chunk 超过 500KB，但构建通过。
- v1.8.3：通过，后台 `npm run build` 成功；日志目录 `.devhub/checks/20260518-194453/`。
- v1.8.3-S1：通过，后台 `npm run build` 成功；日志目录 `.devhub/checks/20260518-214606/`。
- v1.8.3-S1 操作历史热修：通过，后台 `npm run build` 成功；日志目录 `.devhub/checks/20260518-215550/`。
- v1.8.3-S2：通过，后台 `npm run build` 成功；日志目录 `.devhub/checks/20260518-221921/`。
- v1.8.3-S2 筛选条 UI 修正：通过，后台 `npm run build` 成功；日志目录 `.devhub/checks/20260518-223539/`。
- v1.8.3-S2 二级导航纠偏：通过，后台 `npm run build` 成功；日志目录 `.devhub/checks/20260518-225027/`。
- v1.8.3-S4 浏览器 IA 回归：通过，`scripts/check-admin-plugin-ia.sh` 已打开 5 个治理域、验证旧路由跳转和 1024 宽度；截图目录 `/workspace/.devhub/screenshots/v1.8.3-s4`。
- v1.8.3-S4：通过，后台 `npm run build` 成功；日志目录 `.devhub/checks/20260519-002055/`。
- v1.8.3-S5：通过，后台 `npm run build` 成功；日志目录 `.devhub/checks/20260519-003946/`。
- v1.8.3-S6 浏览器 IA 回归：通过，截图目录 `.devhub/screenshots/v1.8.3-s6`；当前测试数据未返回 `official_announcement`，脚本记录跳过原因。
- v1.8.3-S6：通过，后台 `npm run build` 成功；日志目录 `.devhub/checks/20260519-010359/`。
- v1.8.3-S7 浏览器 IA 回归：通过，截图目录 `.devhub/screenshots/plugin-ia`，报告文件 `.devhub/screenshots/plugin-ia/report.json`；`official_announcement` 已由 fixture 强制覆盖。
- v1.8.3-S7：通过，后台 `npm run build` 成功；日志目录 `.devhub/checks/20260519-014314/`。
- v1.8.3-S7 Go 检查：`gofmt` 已执行，`go test ./...`、`go build ./...` 通过。
- v1.8.3-S8：通过，插件包 migrations 规范收口相关 Go 单测通过；`go test ./internal/service ./internal/plugins`、`go build ./...` 通过。
- v1.8.3-S9：新增 PluginRegistry reload 单测覆盖启停、子站状态、配置刷新和 reload 失败保留旧快照；按本轮最低要求执行 Go 检查。
- v1.8.3-S10：新增声明型插件可用闭环单测；`gofmt` 已执行，`go test ./...`、`go build ./...`、`./scripts/check-frontend.sh --admin-only --quick` 均通过；后台 quick 日志目录 `.devhub/checks/20260519-032153/`。
- v1.8.3-S11：新增 external_service 单测覆盖 endpoint 校验、warning / recovery、disabled skipped 和 `hook_executions(service_type=external_service)` 记录；`gofmt` 已执行，`go test ./...`、`go build ./...`、`git diff --check`、`./scripts/check-frontend.sh --admin-only --quick` 均通过；后台 quick 日志目录 `.devhub/checks/20260519-045723/`。
- v1.8.3-S12：插件包 upload -> promote -> install 闭环继续收口。blocked / failed 上传包不可 promote；promote 只把包转入 `storage/plugins/packages/` 本地仓库，不安装、不启用、不执行脚本；本地仓库列表展示 `source_upload_id/promoted_at` 以追溯来源上传包；install 只能从本地仓库包执行，且必须携带当前 install dry-run 计划凭证 `dry_run_id`，服务端会重新 dry-run 并核对 path / plugin_code / version / manifest checksum / checksum status / migration plan hash，upload/staging 阶段旧结果不能直接复用。dry-run 仍为只读计划预览，不执行 SQL；install 只基于 `migrations/` 计划执行，根目录 `001_schema.sql` 不执行。后台安装弹窗和测试验收清单已同步更新。
- v1.8.3-S12 检查：`gofmt` 已执行，`go test ./...`、`go build ./...`、`./scripts/check-frontend.sh --admin-only --quick` 均通过；后台 quick 日志目录 `.devhub/checks/20260519-095725/`。
- v1.8.3-S13：新增真实插件包 fixture 验收 S12 链路。`scripts/build-plugin-package-fixtures.sh` 可生成 `devhub-fixture-valid-plugin*.zip`、`devhub-fixture-blocked-plugin*.zip`、`devhub-fixture-deprecated-schema-plugin*.zip`；`web/admin-app/tests/e2e/plugin-package-real-fixtures.spec.js` 通过真实 Admin API 覆盖 upload -> precheck -> blocked / promotable -> promote -> local repository -> install dry-run -> install，并打开后台插件包治理页做 blocked / 本地仓库 smoke。验收确认 blocked 包后端 promote 拒绝、promote 不安装 / 不启用 / 不执行 SQL / scripts、upload 暂存包不能直接 install、本地仓库包缺少或不匹配 `dry_run_id` 会拒绝 install、dry-run 不执行 SQL、install 只基于 `migrations/`，根目录 `001_schema.sql` 只 warning 不执行，安装成功后 PluginRegistry reload。
- v1.8.3-S13 检查：`bash -n scripts/build-plugin-package-fixtures.sh`、`bash -n dev.sh`、fixture 生成脚本、真实插件包 E2E、`go test ./...`、`go build -o .devhub/devhub .`、`git diff --check`、`./scripts/check-frontend.sh --admin-only --quick` 均通过；后台 quick 日志目录 `.devhub/checks/20260519-120918/`。
- v1.8.3-S14：external_service non-blocking Webhook 投递闭环。声明型插件 Hook 声明新增 `service_type=external_service`、`path`、`method`、`retry_enabled`、`max_attempts`、`enabled`；HookBus 触发后异步 `POST {endpoint_url}{hook.path}`，主流程不等待远端响应；投递记录写入 `hook_executions` 并支持 timeout、auth_type=none/bearer、retry、failure_policy、health warning/error 和 disabled / archived / 子站 disabled skipped。token 明文、Authorization Header、Webhook Secret、Callback Token 和敏感 payload 不进入执行记录、日志或审计；blocking Hook 仍未实现。
- v1.8.3-S15：真实声明型插件“安装到使用”完整业务闭环。新增 `devhub-fixture-links-plugin*.zip` fixture，声明 `official_links*`、`friend_link*`、插件菜单、权限、配置 Schema 和 `migrations/001_init.sql`；新增后台 E2E 通过真实 Admin API 验收 upload、precheck、promote、本地仓库 install dry-run、install、PluginRegistry reload、全局启用、子站启用、菜单可见、权限矩阵、配置读写、内容创建、板块 / 子站 / 全局 / 归档阻断和历史内容可读。后台 `admin/posts` 显式声明型 `category_id/content_type/plugin_code` 创建路径继续走 Service 层校验，不回退为 `core`。
- v1.8.3-S15 检查：`gofmt` 已执行；`bash -n scripts/build-plugin-package-fixtures.sh`、`./scripts/build-plugin-package-fixtures.sh --suffix s15_smoke`、`go test ./internal/transport/httpapi -run TestAdminPostCreateSupportsDeclarativePluginContentType -count=1`、`node --check web/admin-app/tests/e2e/plugin-declarative-install-use.spec.js`、`go build -o .devhub/devhub .`、`docker compose run --rm admin-e2e npx playwright test tests/e2e/plugin-declarative-install-use.spec.js`、`go test ./...`、`go build ./...`、`git diff --check`、`./scripts/check-frontend.sh --admin-only --quick` 均通过；后台 quick 日志目录 `.devhub/checks/20260519-163042/`。后台构建仍提示既有 `index/Content/json-editor-vue` chunk 超过 500KB，但构建通过。
- v1.8.3-S16：后台插件治理复杂度继续压缩。5 个治理域保持不变，默认界面更轻；插件包治理改为流程型工作台，Webhook 治理聚焦投递和异常处理，插件详情抽屉压缩为概览、配置、能力、运行和技术详情。`scripts/check-admin-plugin-ia.sh` 已同步新抽屉结构并通过浏览器回归，截图目录 `.devhub/screenshots/plugin-ia`。
- v1.8.3-S16 检查：`./scripts/check-admin-plugin-ia.sh` 通过；`./scripts/check-frontend.sh --admin-only --quick` 通过，后台 quick 日志目录 `.devhub/checks/20260519-174754/`。后台构建仍提示既有 `index/Content/json-editor-vue` chunk 超过 500KB，但构建通过。
- v1.8.3-S19：external_service Webhook 可交付闭环。新增官方样板包 `examples/plugins/official_webhook_notify`，新增手动重试 API，后台插件详情新增 external_service 配置表单，Webhook 治理新增“外部服务执行”Tab。安全边界保持不变：不执行第三方代码、不开放 blocking Hook、不回显 token / secret、不改变 Webhook Secret / Callback Token 安全模型。
- v1.8.3-S19 检查：宿主机缺少 `go/gofmt/node`，Go 检查使用 Docker `golang:1.22-bookworm` 执行；`gofmt` 已执行，定向 `go test ./internal/service -run 'TestDryRunPluginPackage_OfficialWebhookNotifyExampleOK|TestExternalServiceManualRetry' -count=1`、`go test ./...`、`go build ./...`、`git diff --check`、`./scripts/check-frontend.sh --admin-only --quick` 均通过；后台 quick 日志目录 `.devhub/checks/20260520-225938/`。首次 Docker `go build ./...` 因容器内 Git safe.directory/VCS stamping 失败，随后在容器内设置 `/workspace` 为 safe directory 后复跑通过；未使用 `-buildvcs=false` 掩盖构建。
- v1.8.3-S21：插件包清理 / 删除入口补齐。上传记录页支持删除 `uploaded/staged/blocked/failed/expired` 并硬删文件和上传记录；blocked / quarantine 包支持单删和批量 cleanup；上传 cleanup 与本地仓库 cleanup 均要求先 dry-run，再携带 `confirm_token` 执行，返回 `will_delete_count/will_free_bytes/items`。本地仓库包允许删除 `storage/plugins/packages/` 下所有未安装包；已安装包禁删并提示先归档 / 软卸载插件。删除路径限制在 uploads / staging / quarantine / packages，不执行插件代码、不执行 SQL。
- v1.8.3-S21 审计：新增 `plugin_package_upload.deleted`、`plugin_package_upload.cleanup`、`plugin_package_quarantine.deleted`、`plugin_package_repository.deleted`、`plugin_package_repository.cleanup`。
- v1.8.3-S21 检查：Docker `gofmt` 已执行；定向 `go test ./internal/service -run 'TestPluginPackageUploadLifecycle_DeletePromotedBlockedAndCleanup|TestDeletePluginPackageRepositoryPackage' -count=1`、`go test ./internal/service -run 'TestDeletePluginPackageRepositoryPackage' -count=1`、`go test ./internal/transport/httpapi -count=1`、`go test ./internal/transport/httpapi -run TestAdminPluginPackageRepositoryCleanupRoutesExist -count=1`、`go test ./...`、`go build ./...`、`./scripts/check-frontend.sh --admin-only --quick`、`git diff --check` 均通过；后台 build 日志目录 `.devhub/checks/20260527-123700/`，显式 repository cleanup 别名补充后复跑日志目录 `.devhub/checks/20260527-124613/`，blocked/invalid 非 promoted 测试包删除规则补充后复跑日志目录 `.devhub/checks/20260527-131421/`，本地仓库所有未安装包可删除规则补充后复跑日志目录 `.devhub/checks/20260527-132144/`。首次 Docker `go build ./...` 因 Git safe.directory / VCS stamping 失败，随后在容器内设置 `/workspace` 为 safe directory 后复跑通过，未使用 `-buildvcs=false`。

完整一键手动测试入口仍为：

```bash
./scripts/test-all.sh --quick
```

补丁质量检查：

```bash
git diff --check
```

结果：通过。

脚本语法检查：

```bash
bash -n scripts/check-frontend.sh
```

结果：通过。

## 后续建议

- 继续把插件详情中“运行记录 / 审计日志”与独立运行时治理页之间的跳转参数补齐。
- 后续可为插件包治理增加更完整的二级 Tab：远程索引、暂存区、预检记录、兼容性检查、安装任务、启用前检查、升级任务、卸载任务。
- 对所有插件治理表格补一轮空状态和错误提示截图验收，避免英文接口错误直接暴露给后台用户。
